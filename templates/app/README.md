# {{APP_NAME}}

Generated by `ziggy create`.

## Structure

- `lib/`: app Zig business logic
- `ios/`: iOS host app
- `android/`: Android host app
- `.ziggy/sdk/`: vendored Ziggy SDK wrappers
- `.ziggy/runtime/`: vendored Zig runtime sources
- `.ziggy/generated/`: generated bridge and plugin registrants
- `plugins/`: local plugin packages

## Commands

```sh
ziggy codegen
ziggy plugin sync .
ziggy run . --once
```
