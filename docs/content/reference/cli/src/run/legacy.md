# `cli/src/run/legacy.zig` (Removed)

_Language: Zig_

`legacy.zig` has been removed and replaced by modular files in:

- `cli/src/run/platform/`
- `cli/src/run/unified/`

Use [`cli/src/run/platform/root.zig`](platform/root.md) as the entrypoint for
platform execution.
