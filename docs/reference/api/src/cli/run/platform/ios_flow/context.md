# `src/cli/run/platform/ios_flow/context.zig`

_Language: Zig_

Shared state for the iOS run orchestration pipeline.

## Public API

### `RunContext` (const)

Shared iOS run state passed between selection and launch phases.

```zig
pub const RunContext = struct {
```
