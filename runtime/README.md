# Ziggy Runtime Sources

This directory is bundled with Ziggy and copied into generated apps under `.ziggy/runtime`.
It contains the Zig runtime sources used to build platform runtime artifacts from app-local state.
