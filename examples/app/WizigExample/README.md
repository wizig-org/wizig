# ZiggyExample

Generated by `ziggy create`.

## Structure

- `lib/`: shared Zig logic entry points
- `plugins/`: local plugin manifests/adapters
- `ios/`: Xcode-compatible iOS host project
- `android/`: Android Studio-compatible host project

## Run

```sh
ziggy run ios ios
ziggy run android android
```
